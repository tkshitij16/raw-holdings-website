import type { Metadata } from 'next'; import { PageShell } from '@/components/site-shell'; import { InnerHero } from '@/components/inner-hero'; import { ContactForm } from '@/components/contact-form'; import { site } from '@/src/content/site';
export const metadata:Metadata={title:'Discuss a Project'};
export default function Page(){return <PageShell><main id="main"><InnerHero kicker="RAW / CONTACT" title="Discuss a Project" intro="Tell us what you are working on and where RAW Holdings may fit."/><section className="section contact-layout"><div className="shell"><aside><p className="meta">DIRECT CONTACT</p><a href={`tel:${site.phone.replaceAll('-','')}`}>{site.phone}</a><p>{site.hours}</p><p>{site.location}</p><p className="note">Email destination pending client confirmation. This demo validates inquiries without sending them.</p></aside><ContactForm/></div></section></main></PageShell>}
